node_modules/
dist/
coverage/
package-lock.json

# Os .md do projeto são formatados à mão — tabelas alinhadas, quebras de linha
# escolhidas. Uma passada do Prettier reflui tudo e gera um diff gigante sem
# ganho nenhum de legibilidade. O Prettier aqui cuida do código.
*.md

# Gerada pelo tests/planilha.test.ts a cada `npm test` (P3-02). Formatar à mão
# criaria um laço: o Prettier reflui, a próxima suíte regrava sem reflow, e o
# `format:check` volta a acusar. Quem define o formato é o gerador.
docs/planilha/
